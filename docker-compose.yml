version: '3.8'

services:
  # Redis — for real-time queue state and Bull.js job queues
  redis:
    image: redis:7-alpine
    container_name: qflow_redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    restart: unless-stopped

volumes:
  redis_data:
